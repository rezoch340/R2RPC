# R2RPC 项目进度台账

> **唯一进度真源。** 记录当前后端、管理前端、验证基线和后续状态；旧 Go 系统语义基线已归档到 `docs/archive/旧版-Go-核心功能统计.md`。
>
> **更新规则(进度永不丢):** 每个功能在自己的分支上做,**完成一项就在本文件把它勾成 ✅ 并补「完成记录」**,和代码一起进同一个 PR 合入 main。进度跟着 git 走,不依赖会话记忆。
>
> 详细语义参考:`docs/R2RPC-核心功能统计.md`;分布式/冷热路径与 SDK 约定:
> `docs/design-conventions.md`。当前交接范围:`docs/R2RPC-新版开工提示词.md`。

**图例:** ✅ 已完成 · 🚧 进行中 · ⬜ 待办

---

## 当前验证基线（2026-07-25）

- 后端 backlog #1–#15 与全栈交付 #16–#21 保持全部 ✅。
- `pnpm smoke` / `pnpm test:e2e` 已统一为纯 HTTP/WebSocket 黑盒完整性测试。
- 隔离 PostgreSQL/Redis/Manticore + 真实 API + Worker 环境：**180 passed, 0 failed**。
- 覆盖全部 HTTP controller 方法、登录/读取/拒绝/写入系统审计、安全字段/权限委派、权限组编辑/嵌套权限/用户分组/root 写隔离、用户资料/改密和
  管理员写隔离，以及 WS 鉴权、heartbeat、服务端 ping、20 秒读超时、4 MiB 帧限制、拒分片、
  RPC result 身份匹配/去重、AppAudit Step、手动 RPC 权限/调用/审计和 maxInFlight。
- Device Token 为无过期时间的设备长期凭证，只能撤销或软删除；Access Token 支持可选的
  绝对过期时间与最大 RPC 调用次数，并可二次编辑且不重置累计次数。数据库当前为 11 个
  Drizzle 迁移、15 张表。
- maxInFlight 通过 256 个并发 HTTP invoke 真实占槽验证，不再用 Redis 直连结果代替 E2E。
- `test/assert-blackbox-e2e.js` 强制禁止 E2E 导入持久层或应用内部 service。
- retention/stale/metrics/maxInFlight 的直连脚本已改为 `*.integration.ts` / `test:integration:*`，只作为非 E2E 内部算法检查。
- `pnpm lint:check` 已加入可读命名和控制流门禁：无单/双字母及团队禁止缩写，圈复杂度 ≤ 10、嵌套 ≤ 3、单函数语句 ≤ 40。
- Jest **10 suites / 35 tests passed**；OpenAPI 当前 **39 个 HTTP 路径模板 / 52 个操作 /
  54 个 schema**，全部具有说明、成功响应 schema、标准错误响应和明确鉴权方案。
- 前端 Playwright **12 passed**；只访问 UI 与公开 HTTP API，覆盖手动 RPC 调试、筛选分页、
  两类令牌作用域编辑、Access Token 时间/次数策略编辑、复制回退、日志详情、系统日志长描述
  列宽隔离、导航预取和登录保护。
- JavaScript SDK **10 tests passed**；Android SDK **8 tests passed**。两端均覆盖设备
  Job/Result、调用方 HTTP、Action 超时和 AppAudit Recorder。
- 受限 Compose 性能测试 **4 devices / 1600 requests / 0 failures / 80.03 req/s /
  P95 7.50 ms**；全部设备收到 Hello，服务 CPU 上限合计 4.00 核、内存 3840 MiB。
- 安全策略、贡献指南、行为准则、许可与上游归属、发布流程、Issue/PR 模板和 SDK 制品元数据
  已补齐；GitHub Private Vulnerability Reporting 已启用。

---

## 总览

| # | 事项 | 优先级 | 状态 | 依赖 |
|---|------|--------|------|------|
| — | MVP 调用闭环 + 三套授权域(设备组/RBAC/access token)+ 全局软删除 | — | ✅ | — |
| 1 | 日志保留/裁剪 | 高 | ✅ | — |
| **2** | **设备接入模型重构(epic)** → device-model-redesign spec | 高 | ✅ | — |
| 2a | └ rename `groups→projects` | 高 | ✅ | — |
| 2b | └ device token(新表 + CRUD + 在线设备数) | 高 | ✅ | — |
| 2c | └ 设备自注册 + 删 client-login | 高 | ✅ | 2a,2b |
| 2d | └ 设备持久态(stale 扫描 + 列表/详情 API + platform/ip/extra) | 高 | ✅ | 2c |
| 3a | 指标聚合管道(日聚合表+累加+对账+清理) | 高 | ✅ | 2d |
| 3b | 指标读视图(weekly + trend,读聚合表) | 高 | ✅ | 3a |
| 4 | maxInFlight 在途并发限流 | 高 | ✅ | — |
| 5 | WS 服务端 ping + 读超时判离线 | 中 | ✅ | — |
| 6 | WS 单帧上限 + 拒分片 + deadline 过期丢弃 | 中 | ✅ | — |
| 7 | 用户 enabled + last_login_at | 中 | ✅ | — |
| 8 | 分组 enabled + GroupInfo 派生统计 | 中 | ✅ | 2d,#3 |
| 9 | 监控筛选下拉选项 | 低 | ✅ | — |
| 10 | rpc 状态细分 `rejected` | 低 | ✅ 随 #4 | #4 |
| 11 | client login `platform`/`extra` 落库 | 低 | ✅ 随 2d 落齐 | 2c |
| 12 | 设备上报 AppAudit V1 日志 Step | 中 | ✅ | 请求日志冷路径 |
| 13 | 管理员账号隔离与改密 | 高 | ✅ | RBAC / users |
| 14 | 权限组与 root-only RBAC 写隔离 | 高 | ✅ | RBAC / users |
| 15 | 系统操作审计日志 | 高 | ✅ | 后台控制面 / users |
| 16 | 管理前端 | 高 | ✅ | 后端公开 HTTP API |
| 17 | 手动 RPC 调试与独立权限 | 高 | ✅ | RPC / RBAC / 管理前端 |
| 18 | 后台授权公共 cache-aside | 中 | ✅ | JWT / RBAC / Redis |
| 19 | 前后端统一配置与完整 Compose | 高 | ✅ | API / Worker / Frontend / Deploy |
| 20 | Docker 性能测试与 4 核 4 GiB 资源预算 | 高 | ✅ | #19 / 公开 HTTP API |
| 21 | Android/Kotlin 与 JavaScript/TypeScript SDK | 高 | ✅ | 设备 WS / 公开 RPC / AppAudit |
| 22 | GitHub Actions CI 与 GHCR 镜像发布 | 高 | ✅ | #19 / 全栈测试 |

---

## 大块(核心能力)

### ✅ #1 日志保留/裁剪
`request_logs` 是日志表(硬删)。按天清理(默认 3 天)+ 每
`(project,action,clientId)` scope 只留最新 100 条(窗口函数)，挂现成 5min 维护
worker(`retention-sweep`)。阈值走 zod 配置。
> **聚合表按天清理**(`AGGREGATE_RETENTION_DAYS`,§2.8 item 3)暂缓 → 并入 #3(表还没建)。

### ✅ #2 设备接入模型重构(epic)
**设计定稿:** `docs/superpowers/specs/2026-07-09-device-model-redesign-design.md`。把"设备持久态"扩成一次接入模型重构:device token 自注册(SDK 自生成 id)、`groups→projects` 改名、删 client-login/clients/client_groups。设备 project **继承 device token**(建时勾定,天然绑定,不自报能力)。在线态走 Redis 热 + PG(`devices`)冷持久。
**子项(各自独立 spec/PR):**
- **2a** ✅ rename `groups→projects`(纯机械,先做,后续全用新名)。
- **2b** ✅ device token:`device_tokens` + `device_token_projects` + CRUD + 在线设备数。独立无依赖。
- **2c** ✅ 设备自注册 + 删 client-login(WS 网关改 device-token 鉴权 + 自生成 id → upsert `devices` + presence;删 `clients`/`client_groups`/client-login;改 RBAC/seed/smoke)。**最大一块。**
- **2d** ✅ 设备持久态:stale 扫描 worker + 列表/详情 API(`read/device`)+ platform/ip/extra 落齐(并入原 #11)+ `status` 口径。
**解锁:** #3(device 日聚合)、#8。

### ✅ #3 指标聚合体系(3a + 3b)
- **3a ✅ 聚合管道**:两日聚合表 `device_daily_metrics`/`rpc_daily_metrics`(drop `metrics` 空壳)+ 完成累加(挂 `RequestLogProcessor`,writeSpine 首插判去重)+ 重启对账(worker 启动从 request_logs 重灌最近 N 天)+ 聚合表按天清理(30 天,承接 #1 暂缓项)。
- **3b ✅ 读视图**:`GET /metrics/weekly`(近7天设备)+ `GET /metrics/trend`(按天补零 + successRate)读聚合表 API;`overview` 不动(扫 raw,retention 封顶 3 天、成本有界)。**GroupSummary/GroupInfo 归 #8**(有设备维度,不在 3b 重复)。
**依赖:** 3b 依赖 3a;整体依赖 #2(设备聚合口径,已就绪)。

### ✅ #4 maxInFlight 在途并发限流
设备 WS 连接 `?maxInFlight` 自报,服务端夹到 `[256,1024]`(默认 512),存 Redis `device:maxinflight:{cid}`(TTL 随 presence)+ devices 行 + welcome 回带。在途计数 Redis `device:inflight:{cid}`:invoke 选到设备后 `tryAcquireSlot`(INCR,超上限自减回退→`rejected`/429),派发段 try/finally `releaseSlot` 保证每 acquire 精确一次 release;连接 reset + 下线 offline 清理防泄漏。
> 组饱和(多设备轮询跳过满设备)已补齐:invoke 无 `?clientId=` 时走 `PresenceService.pickOnlineAcquire`,组内 RR 从游标起轮一圈、占第一个未满设备的槽,全满才 `rejected`/429(`功能组内设备在途任务均已满`);指定 clientId 路径仍单设备占槽。见「完成记录 2026-07-09 · #4b」。

## WS 协议健壮性

### ✅ #5 服务端主动 ping + 读超时
按 socket 每 5s 主动 `ping`;活动(message/pong)超 20s 无更新即 `terminate()` 判离线。下线清 timer。

### ✅ #6 帧限制 + 拒分片 + deadline
单帧 **4 MiB 上限**(`@WebSocketGateway maxPayload`,超限 ws 自动 close 1009);job `deadlineAt`(=invoke startedAt+timeout),`deliverLocalJob` 发送前 guard 丢弃过期任务。
**拒分片(FIN=0)** 已补齐(见 #6b):网关 `handleConnection` 首行实例级包裹 `socket._receiver.getInfo`,帧头解析后 `_fin===false` 即用 ws 自身错误链路以 **1009** 关闭整条连接(控制帧强制 FIN,故只命中数据帧分片);依赖 ws 8.21 内部字段,升级失踪则 fail-open 记 error。

## 账户 / 分组管理

### ✅ #7 用户 enabled + last_login_at
用户 `enabled` **双重执行**（登录时 403 + JWT 授权快照校验吊销现有 JWT）+
`last_login_at`（登录更新）；`POST /users/:id/enabled` 启停后立即失效该用户授权缓存，
list/detail 暴露。(`can_rpc` 已被 RBAC 取代,算演进。)

### ✅ #8 分组 enabled + GroupInfo
分组 `enabled`(启停;**invoke 派发点校验**,禁用组返 `disabled` 拒派)+ `GET /projects/info` GroupInfo 派生统计(设备数/在线数/近7天请求/成功率/lastSeen/运行态 disabled·no_device·online·stale·offline)。设备计数走 devices→device_tokens→device_token_projects join;7天量读 rpc_daily_metrics;在线数用 `devices.online`(stale worker 保鲜)。**依赖 #2、#3 已就绪。**

## 小项

### ✅ #9 监控筛选下拉选项
`GET /monitor/request-options?project&action&clientId`(`read/monitor`)→ `{projects, actions, clientIds}`(均 string[])。从 `request_logs` 去重取三类候选,**联动过滤**(每维排除自身、按其余已选维约束),实体存在要求:project inner join `projects`(alive)、client inner join `devices`(alive),action 无实体不约束;每类封顶 200。对齐老系统 `RequestFilterOptions`。

### ✅ #10 rpc 状态细分 `rejected`
`rejected`(429,设备在途已满)随 **#4** 交付,落 `request_logs.status`。(组饱和精确判见 #4 的延后 refinement。)

### 🔀 #11 → 并入 2d
platform/extra 落库随设备接入重构一起做(见 2d);client-login 本身被删,改为设备自注册时落。

### ✅ #12 设备上报 AppAudit V1 日志 Step
设备随最终 WS `result.appAudit` 批量上报 metadata 和成功/失败 Step。服务端执行 V1
schema、连续 sequence、最多 64 metadata/128 Step、512 KiB 体积校验；非法审计整体丢弃但
RPC 正常完成。合法审计随 `RequestLogJob` 进入 Worker，只保存到 Manticore
`app_audit_json`，Monitor 详情懒加载返回，PG 脊柱和列表不包含审计。

### ✅ #13 管理员账号隔离与改密
新增 `PATCH /users/:id` 资料修改和 `PATCH /users/:id/password` 改密。
`AdministratorAccountPolicyService` 以 JWT 请求者编号保护 `isRoot` 目标：非本人一律 403，
且统一覆盖资料、密码、enabled、软删除和 RBAC 用户角色绑定/解绑。用户响应显式选择安全字段，
不返回 `passwordHash`；`users.role` 继续只作遗留展示，不参与授权或保护。

### ✅ #14 权限组
复用现有四张 RBAC 表，将 `roles` 明确为权限组。权限组列表和用户已分配组返回嵌套
permissions；新增权限组编辑、用户权限组查询和请求体关联入口，旧 URL 入口继续兼容。
`read/rbac` 可委派读取，全部 RBAC 写操作叠加 `RootGuard`，只有种子管理员可执行；
`manage/rbac` 不能绕过身份闸。组内权限使用固定批量查询组装，无 N+1，无数据库迁移。

### ✅ #15 系统操作审计日志
新增不可变 `system_logs`，每条记录包含操作 `name/description`、操作者用户名快照、动作、对象、
白名单 metadata、HTTP 结果、IP 和时间。登录成功/失败、全部 JWT 控制面读取和 Guard/路由
阶段拒绝自动记录；用户、project、两类 token 与 RBAC mutation 显式增加 `@SystemAudit`。
`GET /system-logs` 使用 `read/system-log` 筛选分页。日志不保存密码/token 明文，不提供修改
或删除 API，也不与 RPC 请求日志、设备 AppAudit 混用。

### ✅ #16 管理前端
使用 Next.js + shadcn 新建独立 `frontend/`，所有类型、路由和权限
语义都按 R2RPC 当前公开 API 重写。页面覆盖运行概览、功能组、设备、两类令牌、请求日志与
AppAudit Step、后台账号、权限组和系统日志。前端只能打 HTTP API，具有独立 E2E 黑盒边界
守卫和完整变量名门禁；生产镜像与统一配置已完成。Playwright 已在隔离
PostgreSQL/Redis/Manticore + 真实 API/Worker 上完成浏览器黑盒复跑。
令牌与 JSON 复制统一使用公共 `CopyButton`，非安全上下文自动回退；运行概览使用折线趋势图，
请求详情采用宽版右侧抽屉，AppAudit Step 默认收起。

### ✅ #17 手动 RPC 调试与独立权限
新增 `invoke/manual-rpc` 内置权限和完整说明；19 条内置权限说明均由种子脚本幂等同步。
控制面新增 `GET /rpc/debug/options` 与 `POST /rpc/debug/invoke/:project/:action`，后台 JWT
必须通过独立权限，公开 Access Token RPC 边界保持不变。调用复用真实设备派发链路，请求日志
记录 `requesterUserId`，系统日志记录功能组、Action、设备和超时但不记录 Payload。
前端新增 `/rpc-debugger` 页面与权限导航，可选择功能组、历史 Action、在线设备和超时，编辑、
格式化 Payload，并查看请求预览、响应、状态和耗时。

### ✅ #18 后台授权公共 cache-aside
新增公共 `RedisCacheAsideService`，统一 `getOrLoad`（读 Redis → 未命中/异常回源
PostgreSQL → 回写 Redis）和 `writeAndInvalidate`（数据库写成功 → 删除缓存）；
Access Token、Device Token 与后台用户授权不再各自重复实现缓存轮子。用户身份与权限快照
默认 TTL 60 秒、配置范围 60–300 秒。权限组挂载/移除权限、权限或权限组删除、用户分组/移组、
账号启停和软删除都会立即失效所有受影响用户缓存。

### ✅ #19 前后端统一配置与完整 Compose
API、Worker、Drizzle、迁移、种子和前端统一读取 `config.yaml` schema；CORS、前端 API、
开发来源与种子管理员不再使用分散环境变量。新增后端生产 Dockerfile，并以根目录
`compose.yaml` 编排 PostgreSQL、Redis、Manticore、migration、seed、API、Worker 与 frontend。
统一配置只读挂载，应用镜像以非 root 用户运行。

### ✅ #20 Docker 性能测试与资源硬限制
新增统一配置驱动的固定速率性能执行器，通过公开 API 创建临时令牌并挂载 4 台 WebSocket
设备。10 个场景覆盖控制面读取、手动自动路由 Hello、Access Token 自动轮询和随机指定设备
Hello；响应契约及全部设备覆盖也属于强制阈值。Compose 新增 `performance` profile，全部
服务 CPU、内存和 PID 声明上限合计 4.00 核、3840 MiB。

### ✅ #21 Android 与 JavaScript SDK
新增 `sdk/javascript` 与 `sdk/android`，两端都提供设备常驻连接、调用方 HTTP 客户端和
AppAudit V1 Recorder。设备端封装 welcome、heartbeat、鉴权失败停止、异常断线指数退避、
Action 路由/超时和规范 result；调用方封装自动/指定设备 invoke 与在线状态查询。JavaScript
输出 ESM/TypeScript 声明，Android 输出 Maven AAR；测试只走公开 HTTP/WebSocket 协议。

### ✅ #22 GitHub Actions CI 与 GHCR 发布
Pull Request 和 `main` 推送自动执行后端/前端质量门禁，并在完整 Compose 环境运行后端
HTTP/WebSocket 与前端浏览器黑盒。合法 `v*` 标签参考 FlowCore 流程发布 backend/frontend
版本镜像和 `latest` 到 GHCR，单镜像限制 500 MiB。SDK 不进入自动构建矩阵。

---

## 完成记录(倒序)

### 2026-07-25 · #22 GitHub Actions CI 与 GHCR 发布
- 新增 `.github/workflows/ci.yml`：后端执行命名/ESLint、Prettier、构建、Jest、OpenAPI 与
  Drizzle 漂移检查；前端执行命名/ESLint 和生产构建。
- 质量门禁通过后，以完整 Compose 启动 PostgreSQL、Redis、Manticore、migration、seed、
  API、Worker 和 frontend，执行 180 项后端黑盒与 12 项 Playwright 黑盒；失败时输出服务
  日志并上传浏览器失败制品。
- 新增 `.github/workflows/publish-ghcr.yml`，参考 FlowCore 的固定 Action 提交、标签校验、
  Buildx 缓存、500 MiB 大小门禁和版本/`latest` 双标签策略，只发布后端与前端镜像。
- Android 与 JavaScript SDK 不进入 GitHub Actions 构建或发布矩阵。

### 2026-07-25 · Access Token 按调用次数过期与完整二次编辑
- `access_tokens` 新增可选 `maximum_usage_count` 与累计 `usage_count`；迁移
  `0010_lazy_plazm.sql` 保证上限为正数、累计值非负。
- 只有通过鉴权与 DTO 校验的公开 RPC invoke 消耗次数，在线设备查询不消耗；业务层受理后
  无论成功、无设备、设备错误或超时都计数。受限令牌使用 PostgreSQL 条件更新原子占用，
  并发达到上限返回 `429`；不限次数令牌不增加数据库写负担。
- 新增 `PATCH /access-tokens/:id`，在同一事务内二次编辑完整 project 集合、绝对过期时间和
  次数上限；取消或提高上限可恢复调用，编辑不清零累计次数，旧 project 专用入口继续兼容。
- 管理页创建表单增加最大调用次数，列表显示已用/上限，编辑对话框统一修改功能组和两种过期
  策略。OpenAPI、当前有效文档、后端并发黑盒和前端浏览器黑盒同步更新。
- 验证：后端 HTTP/WebSocket 黑盒 **180 passed, 0 failed**，Jest
  **10 suites / 35 tests**，前端 Playwright **12 passed**；前后端 lint/build 与 OpenAPI
  生成全部通过。

### 2026-07-25 · Device Token 无过期时间
- 移除 Device Token 创建参数、响应字段、WebSocket 鉴权过期判断和管理页面过期时间列；
  Access Token 的可选过期时间保持不变。
- 新增迁移 `0009_breezy_pixie.sql` 删除 `device_tokens.expires_at`；Device Token 只通过
  撤销或软删除结束生命周期。
- OpenAPI、当前有效文档和 HTTP/WebSocket 黑盒同步更新；兼容旧调用方多传 `expiresAt`，
  全局参数白名单会忽略该字段，设备仍可正常连接。

### 2026-07-25 · OpenAPI 与发布治理补全
- 运行时 Swagger 和静态 `docs/openapi.yaml` 共用配置、53 个响应 schema 和生成期完整性断言；
  51 个 HTTP 操作全部包含 description、成功响应、标准 4xx/500 和明确 security。
- 安全方案拆分为后台 `adminJwt` 与调用方 `accessToken`；修正 RPC `clientId` 和指标
  `project` 可选查询参数，手动/公开 RPC 的鉴权说明不再混淆。
- AppAudit V1 文档与 OpenAPI 同步 strict object、字符串长度、数字范围、时区、连续序号、
  JSON 序列化和 UTF-8 体积限制。
- 新增许可/归属、安全、贡献、行为、发版、Issue/PR 模板；JavaScript package 与 Android
  Maven POM 补齐仓库、问题入口、SCM、开发者和许可元数据，公共制品继续保持 UNLICENSED。
- 验证：后端 lint/build/OpenAPI/Jest **10 suites / 35 tests**，前端 lint/build，
  JavaScript **3 files / 10 tests + npm pack dry-run**，Android **3 classes / 8 tests +
  release AAR/POM** 全部通过。

### 2026-07-24 · #21 Android/Kotlin 与 JavaScript/TypeScript SDK
- 新增 `@r2rpc/javascript-sdk`，支持 Node.js 20+ 与现代浏览器，包含
  `R2RpcDevice`、`R2RpcCaller`、`AppAuditRecorder`、类型声明和可运行示例。
- 新增 `io.r2rpc:r2rpc-android:0.1.0` Android AAR，使用 OkHttp、Coroutines 与
  kotlinx.serialization，不绑定 Activity，可用于 App、Service 和 Xposed；默认以
  Widevine MediaDrm ID 作为设备 `clientId`。
- 两端统一处理稳定 clientId、Device/Access Token 边界、heartbeat、指数重连、
  未注册 Action、异常、超时、自动路由、指定设备和 AppAudit V1 连续 Step。
- 验证：JavaScript TypeScript/Vitest/build 全绿，**3 files / 10 tests**；Android
  Gradle/JUnit/build 全绿，**3 classes / 8 tests**，MockWebServer 走真实 HTTP/WS。
- 设计：`docs/superpowers/specs/2026-07-24-device-sdks-design.md`；接入：
  `sdk/README.md`。

### 2026-07-24 · #20 Docker 性能测试与 4 核 4 GiB 资源预算
- `config.yaml` 新增 `performance` 段，集中校验目标地址、功能组、虚拟设备数、预热/测量
  时长、并发、目标速率、请求超时、错误率、P95、最小吞吐和结果路径。
- `backend/src/scripts/performance*.ts` 使用种子管理员登录，通过公开 API 创建临时令牌、挂载
  4 台设备并运行 10 个 HTTP/WS 场景；结束后关闭连接并删除临时令牌。
- Compose 新增 `performance` profile；9 个服务资源上限合计 4.00 CPU、3840 MiB，实际
  Docker `HostConfig` 已核验。
- 验证：受限隔离全栈完成 **4 devices、1600 requests、0 failures、80.03 req/s、
  P95 7.50 ms、P99 10.31 ms**；3 个 Hello 场景各 160 次且全部设备收到任务。

### 2026-07-24 · #19 前后端统一配置与完整 Compose
- 根目录 `config.example.yaml` 与 `deploy/config.example.yaml` 共用同一 schema；前者用于
  宿主机，后者只把基础设施地址切换为 Compose 服务名。真实 `config.yaml` 均由 Git 忽略。
- 后端统一 loader 支持向上查找和 `CONFIG_FILE` 显式选择，zod 同时校验 app、frontend、
  db、redis、jwt、manticore、bootstrap 和 retention；前端只向浏览器注入 API 白名单。
- 删除 `CORS_ORIGIN`、管理员账号环境变量、`NEXT_PUBLIC_*`、开发 Origin 环境变量、
  `frontend.yaml` 和 `.env.local.example`。
- 新增 API/Worker 共用的多阶段生产镜像；Compose 使用 migration/seed 一次性任务、基础设施
  健康检查和 API 健康依赖控制完整启动顺序，不固定容器名。
- 验证：Jest **10 suites / 35 tests**、完整 HTTP/WebSocket 黑盒
  **172 passed, 0 failed**、前端 Playwright **12 passed**；前后端 build/lint、Compose
  config、API/frontend 镜像构建、容器内配置加载、迁移与种子全部通过。

### 2026-07-24 · #18 后台授权公共 cache-aside
- 新增 `RedisCacheAsideService.getOrLoad/writeAndInvalidate` 和集中缓存键，zod 契约异常会按未命中
  回源；Redis 故障 fail-open，不阻断 PostgreSQL 鉴权或权威写入。
- JWT 用户身份与权限快照默认缓存 60 秒；RBAC 权限关系、用户分组、账号 enabled 和软删除写入
  成功后删除相关用户缓存，现有 JWT 立即按最新授权执行。
- Access Token Guard、Device Token WS 鉴权与两类令牌写入也复用公共组件，删除重复的
  Redis 读取、回写和缓存失效实现。
- 新增公共组件 7 项单元覆盖和纯 HTTP/WS 授权缓存实时失效场景；Jest
  **9 suites / 31 tests passed**，完整黑盒 **172 passed, 0 failed**，OpenAPI 仍为
  **39 个路径模板**。

### 2026-07-24 · #17 手动 RPC 调试与独立权限
- 新增 `invoke/manual-rpc`，内置权限增至 19 条；每条都有完整 `description`，幂等种子会更新
  已存在内置权限说明，operator 仍只挂 8 条 `read/*`。
- 新增后台 JWT 保护的调试上下文和调用接口；普通账号缺少权限返回 403，root 或被授权账号可
  选择在线设备或使用功能组轮询完成真实 RPC 往返。
- 手动调用继续进入 BullMQ 请求日志冷路径并记录后台发起人；系统操作审计明确记录调用参数
  白名单，Payload 不入系统日志。公开 Access Token 调用协议未改。
- 新增 `/rpc-debugger` 页面、导航权限与预取；表单支持历史 Action、在线设备、超时、JSON
  格式化，请求/响应使用公共 `JsonBlock` 和兼容复制；重复调用等待期间保留上一份结果，
  新响应到达后原位替换，结果区不闪烁；两个操作按钮保持文案、尺寸、位置和不透明度稳定。
- OpenAPI 当前 **39 个路径模板**；Jest **8 suites / 24 tests passed**；HTTP/WebSocket
  黑盒 **162 passed, 0 failed**；前端 lint/生产构建通过，Playwright **11 passed**。
- 设计：`docs/superpowers/specs/2026-07-24-manual-rpc-debugger-design.md`。

### 2026-07-24 · #16 管理前端
- 使用 Next.js App Router、shadcn、TanStack Query 和数据驱动公共组件结构，
  新建独立 `frontend/`，领域类型、路由和权限语义全部按 R2RPC 当前 API 重写。
- 页面覆盖运行概览、功能组、设备、Access Token、Device Token、请求日志/AppAudit Step、
  后台账号、权限组和系统日志；账号菜单支持本人改密，root 与 RBAC 写入口按后端规则显隐。
- Access Token 与 Device Token 均支持二次编辑功能组；Access Token 立即清除调用鉴权缓存，
  Device Token 通过集群事件断开旧作用域连接并在重连后继承新作用域。
- 全部实体表补齐稳定字段筛选和分页，默认 10 条/页、最大 100 条/页；分页器作为表格页脚，
  支持记录区间、数字页码、每页条数和指定页跳转。表格字体、表头、行距、隔行底色和长内容
  列宽统一收敛，说明、令牌明文、payload 与扩展信息不作为筛选项。
- 请求日志详情使用固定宽度右侧抽屉，设备 AppAudit Step 默认折叠；系统日志增加事件与目标
  筛选，请求日志增加载荷索引与耗时范围筛选。
- 运行概览的近 7 天请求量改为带节点和面积渐变的折线趋势图；令牌与 JSON 载荷统一复用
  `CopyButton`，Clipboard API 在局域网 HTTP 下不可用时自动回退。
- 浏览器 E2E 从登录 UI 获取真实 JWT，只通过公开 HTTP API 加载全部页面；静态边界守卫禁止
  导入后端、数据库、Redis、Manticore 或 SQL。Playwright **10 passed**。
- 前端完整变量名门禁、ESLint、TypeScript 与 Next.js 生产构建通过；该阶段的独立
  `frontend.yaml`、Compose 服务和 CORS 配置随后由 #19 统一配置与完整 Compose 取代。
- 全量后端回归为 Jest **8 suites / 24 tests passed**、HTTP/WebSocket 黑盒
  **155 passed, 0 failed**；同时修复系统审计拦截器在无请求体撤销接口上的空值读取问题，
  并补齐登录、读取和拒绝访问审计。
- 设计：`docs/superpowers/specs/2026-07-24-management-frontend-design.md`。

### 2026-07-24 · #15 系统操作审计日志
- 盘点原有 14 张表：业务实体都有语义名称与 description；关系表、请求日志和聚合表不机械补
  无意义 name。新增 `system_logs` 后共 15 张表。
- 新增迁移 `0008_sudden_james_howlett.sql`；系统日志为不可变追加事实，包含人类可读
  `name/description`、操作者快照、动作、对象、安全 metadata、结果、来源和时间。
- 新增 `@SystemAudit` 与全局拦截器，覆盖用户、project、access token、device token 和
  RBAC 全部业务写入口；扩展后自动覆盖登录成功/失败、JWT 控制面读取，并由全局异常过滤器
  补记 Guard/路由阶段拒绝。
- 新增 `GET /system-logs` 与 `read/system-log`；种子权限现为 18 条，operator 的只读权限为
  8 条。
- 黑盒只通过 HTTP 产生并读取日志，验证“admin 创建用户 xxx”、筛选、普通用户权限和密码不
  泄露；隔离 API + Worker 套件 **155 passed, 0 failed**。
- Jest **8 suites / 24 tests passed**；build、lint、Prettier、OpenAPI 和黑盒边界检查通过；
  OpenAPI 当前 37 个路径模板。
- 设计：`docs/superpowers/specs/2026-07-24-system-audit-logs-design.md`；
  计划：`docs/superpowers/plans/2026-07-24-system-audit-logs.md`。

### 2026-07-24 · #14 权限组
- 基于现有 RBAC 关系复用 `roles`、`permissions`、`role_permissions` 和
  `user_roles`，不新增 schema 或迁移。
- `GET /rbac/roles` 和 `GET /rbac/users/:userId/roles` 批量返回嵌套权限；
  新增 `PATCH /rbac/roles/:id`。
- 增加请求体形式的权限挂载和用户分组 POST，原 URL 形式继续兼容并纳入黑盒。
- 新增 `RootGuard` 覆盖所有 RBAC mutation；黑盒验证非 root 即使拥有 `manage/rbac`
  仍不能创建权限组。RBAC 读入口统一为 `read/rbac`。
- 种子权限增至 17 条，operator 的 `read/*` 权限增至 7 条；部署无迁移，只需重跑
  `pnpm seed:admin`。
- 隔离 PostgreSQL/Redis/Manticore + 真实 API + Worker 完整套件
  **136 passed, 0 failed**；E2E 边界守卫确认只访问 HTTP/WS。
- Jest **6 suites / 14 tests passed**；build、lint、Prettier check、OpenAPI 生成通过；
  OpenAPI 当前 34 个路径模板。
- 设计：`docs/superpowers/specs/2026-07-24-permission-groups-design.md`；
  计划：`docs/superpowers/plans/2026-07-24-permission-groups.md`。

### 2026-07-24 · #13 管理员账号隔离与改密
- 新增共享 `AdministratorAccountPolicyService`；目标为 `isRoot` 且请求者不是本人时返回 403，目标不存在返回 404。
- 增加 `PATCH /users/:id` 和 `PATCH /users/:id/password`；创建用户支持 description，用户名、密码和描述补齐数据库长度上限。
- 用户资料、密码、enabled、软删除和 RBAC 角色绑定/解绑全部接入统一保护，请求者编号只读取 JWT 上下文。
- 黑盒给普通测试账号动态授予 `update/user`、`delete/user` 和 `manage/rbac`，仅通过 HTTP 验证其不能改管理员资料/密码、不能停用/删除管理员、不能变更管理员角色；同时验证普通用户改密后旧密码 401、新密码可登录。
- 隔离 PostgreSQL/Redis/Manticore + 真实 API + Worker 完整套件 **131 passed, 0 failed**；E2E 边界守卫确认只访问 HTTP/WS。
- 该阶段 Jest **5 suites / 11 tests passed**；build、lint、Prettier check、OpenAPI 生成通过；
  该阶段 OpenAPI 为 32 个路径模板。
- 设计：`docs/superpowers/specs/2026-07-24-administrator-account-isolation-design.md`；计划：`docs/superpowers/plans/2026-07-24-administrator-account-isolation.md`。

### 2026-07-24 · #12 设备上报 AppAudit 日志 Step
- 新增 R2RPC V1 类型和设备输入 zod 校验；WS `result` 支持可选顶层 `appAudit`。
- 审计限制为 512 KiB、64 metadata、128 Step，sequence 必须从 1 连续递增；非法审计只记警告并丢弃，不影响 RPC 结果。
- 请求日志冷路径增加 `appAudit`，Manticore `request_logs` 增加 `app_audit_json`；启动时可为已有索引自动补列。PostgreSQL schema 无变化。
- `GET /monitor/requests/:requestId` 返回审计对象或 `null`，列表继续只返 PG 脊柱。
- 黑盒通过真实 HTTP invoke → WS result+AppAudit → BullMQ Worker → Monitor HTTP API 验证成功/失败 Step 和非法审计隔离，全套 **121 passed, 0 failed**；E2E 未直连任何持久层。
- 单元测试 **8 passed**；build、lint、format check 和 OpenAPI 生成通过。
- 协议：`docs/device-app-audit.md`；设计：`docs/superpowers/specs/2026-07-24-device-app-audit-design.md`。

### 2026-07-24 · 完整性黑盒冒烟
- 将所有 HTTP controller 方法纳入同一套真实运行实例测试，覆盖 auth/users/RBAC/projects/tokens/devices/RPC/monitor/metrics 的成功、校验、权限、启停、撤销和软删除分支。
- WS 覆盖缺 token/伪造/撤销/软删鉴权、welcome、非法消息忽略、heartbeatAck、主动 ping、20 秒读超时、4 MiB 超限 1009、FIN=0 分片 1009、同 clientId session CAS、优雅离线持久态；Device Token 不设置过期时间。
- RPC 覆盖 project 轮询、指定 client、成功/设备失败/无设备/离线/disabled/timeout、非目标设备伪造 result mismatch、合法结果完成和重复结果 late。
- maxInFlight 只通过公开接口验证：256 个并发 HTTP invoke 占满设备，确认轮询跳过满设备、全组饱和 rejected/429、指定满设备 rejected/429，以及 timeout 后槽释放。
- Worker 日志/Manticore/指标结果全部通过 `/monitor/*` 与 `/metrics/*` 读取验证；不允许 E2E 直连 PG/Redis/Manticore。
- 验证：隔离基础设施 + API + Worker，当时 **117 passed, 0 failed**；加入 #12 后为 121，
  加入 #13 后为 131，加入 #14 后为 136，加入 #15 后为 139，补齐登录/读取/Guard 拒绝审计后
  该阶段基线为 155；现行基线见文首。

### 2026-07-09 · #9 监控筛选下拉选项 — 后端 backlog 收官
- `GET /monitor/request-options?project&action&clientId`(`read/monitor`)→ `{projects, actions, clientIds}`。`RequestLogsService.filterOptions` 三条 `selectDistinct`,联动过滤(每维排除自身、按其余已选维约束),project inner join `projects`(alive)、client inner join `devices`(alive)、action 无实体约束;`client_id` 非空;每类 `limit 200`。MonitorService 透传、controller 加端点 + `QueryRequestOptionsDto`。'monitor' 权限已存,无需改 seed。
- 验证:build/lint/format 绿;e2e smoke +4 断言(projects 含 cn-nodes、actions 含 echo、clientIds 含在线设备、联动 action 无日志→projects 空;冷路径先轮询等落库避 flaky),**72 断言全绿**(起真环境 API+Worker 全套复跑)。
- **当时后端 backlog(#1–#11)全部 ✅**；当前又完成 #12 AppAudit。

### 2026-07-09 · #6b 拒分片(#6 延后项补齐)
- 网关 `handleConnection` 首行 `rejectFragmentedFrames(socket)`:实例级包裹 `socket._receiver.getInfo`(ws 8.21 内部,帧头同步解析处),原调用后若 `_bufferedBytes>=2 && !_errored && _fin===false` → `cb(receiver.createError(..,1009,'WS_ERR_FRAGMENTED_FRAME'))`,复用 ws 自身错误链路以 **1009** 关闭整条连接。
- 控制帧(ping/pong/close)协议强制 FIN,故解析后 `_fin===false` 只可能是数据帧首片/延续帧;`hadHeaderBytes` 门槛避开不足 2 字节时 `_fin` 陈旧值误判。
- 依赖 ws 内部字段(锁定 8.21);升级后 `getInfo` 失踪则 **fail-open** 记一条 error(连接照常,maxPayload+读超时仍兜 DoS 面),不因内部变更把设备连接搞崩。
- 验证:build/lint/format 绿;e2e smoke 新增一条(独立 clientId 探针发 `{fin:false}` 分片帧 → 服务端 1009 关闭),**68 断言全绿**。

### 2026-07-09 · #7 用户 enabled + last_login_at — PR #17
- users 加 `enabled`(启停)+ `last_login_at`;迁移 `0007`。`POST /users/:id/enabled`(`update/user`,→16 条);list/detail 暴露 enabled+last_login。
- enabled **双重执行**:登录时(auth.service 密码后拦禁用 403)+ **每请求**(jwt.strategy.validate 经 rbac.findAuthUser 重查,禁用立即吊销现有 JWT,不等 12h 过期)。登录成功更新 last_login_at。
- 验证:build/lint/format 绿;e2e smoke 停用→现有 token 403 + 重登 403 + 启用后可登 + last_login 字段(56 断言全绿),独立复跑。
- 计划:`docs/superpowers/plans/2026-07-09-7-user-enabled-lastlogin.md`。

### 2026-07-09 · #5+#6 WS 健壮性 — PR #16
- **#5**:服务端每 5s 主动 `ping`;按 socket 追踪活动(message/pong),无活动超 20s → `terminate()`。下线清 timer(不泄漏)。
- **#6**:单帧 4 MiB 上限(`@WebSocketGateway maxPayload` 常量,WsAdapter 透传 ws.Server,超限自动 close 1009);job 加 `deadlineAt`,`deliverLocalJob` 发送前 guard 丢弃过期任务(即时派发极少触发,跨实例延迟时兜底)。
- **拒分片(FIN=0)** 当时延后,已由 #6b 补齐(见上)。
- 验证:build/lint/format 绿;e2e smoke 断言收到服务端主动 ping。
- 计划:`docs/superpowers/plans/2026-07-09-5-6-ws-robustness.md`。

### 2026-07-09 · #4 maxInFlight 在途并发限流(+ #10 rejected) — PR #15
- 设备 WS 连接 `?maxInFlight` 自报,服务端夹到 `[256,1024]`(默认 512),存 Redis `device:maxinflight:{cid}`(TTL 随 presence 刷)+ devices 行 + welcome 回带。迁移 `0006`。
- 在途计数 Redis `device:inflight:{cid}`:invoke `tryAcquireSlot`(INCR,超上限自减→`rejected`/429),派发段 try/finally `releaseSlot`(error/unavailable/success/timeout 全覆盖恰好一次);连接 `resetInFlight`、下线 offline 清理防泄漏。
- **#10 rejected 状态**随本项交付。组饱和轮询跳过 = 延后 refinement(见 #4b 已补齐)。
- 验证:build/lint/format 绿;当时的直连检查现已更名 `test:integration:max-inflight`;e2e welcome/devices maxInFlight 断言绿。
- 计划:`docs/superpowers/plans/2026-07-09-4-max-in-flight.md`。

### 2026-07-09 · #4b 组饱和轮询(#4 延后项补齐)
- `PresenceService.pickOnlineAcquire(projectId)`:组内 `listOnline` → RR 从 `rpc:rr:{pid}` 游标起轮一圈,逐台 `getMaxInFlight`+`tryAcquireSlot`,占到第一个未满设备即返 `{clientId}`(槽已占);整圈满返 `'saturated'`,无在线返 `'no_device'`。取代原 `pickOnline`(已删,唯一调用方是 invoke)。
- `rpc.service.invoke` 把"选设备"与"占槽"合并成一段 try:指定 clientId → 在线校验 + 单设备占槽(满 `rejected`/429);无 clientId → `pickOnlineAcquire`(`no_device`/503、`saturated`/429、命中则 clientId=picked)。占槽成功到出 try 之间无 await,catch 不泄漏已占槽;到达 dispatch try 时槽必已占,finally `releaseSlot` 精确一次。
- 验证:build/lint/format 绿;`test:integration:max-inflight` 覆盖空组、跳过满设备和全满;当前黑盒 E2E 另用 256 个 HTTP invoke 覆盖同一行为。

### 2026-07-09 · #8 分组 enabled + GroupInfo — PR #14
- projects 加 `enabled`(启停);invoke 派发查 `findEnabledIdByName`,禁用组返 `disabled`(HTTP 200 body.status)拒派;`POST /projects/:id/enabled` 启停(`update/project` 权限,→15 条);迁移 `0005`。
- `GET /projects/info` GroupInfo:每 project 设备数/在线数(devices join device_token_projects,online 用 `devices.online`)/近7天请求·成功·成功率(rpc_daily_metrics)/lastSeen + 运行态(disabled>no_device>online>stale>offline,doc §174 顺序)。3 查询 JS 合并不 N+1;ProjectsModule import MetricsModule(无环)。
- 验证:build/lint/format 绿;e2e smoke +6 断言(GroupInfo online、停用拒派、status=disabled)全绿,独立复跑。
- 计划:`docs/superpowers/plans/2026-07-09-8-project-enabled-groupinfo.md`。

### 2026-07-09 · #3/3b 指标读视图 — PR #13(#3 收尾)
- `GET /metrics/weekly`(近7天按 clientId×project 汇总 `device_daily_metrics`,avg=Σlatency/Σtotal,可选 ?project)。
- `GET /metrics/trend`(近 days 天按天汇总 `rpc_daily_metrics`,JS 生成 UTC 日期序列补零,含 successRate;?days 1-90 默认7,可选 ?project)。均 `read/metrics`,mirror 老系统 WeeklyMetric/TrendPoint。`overview` 不动。
- 验证:build/lint/format 绿;e2e smoke 加 weekly/trend 形状+补零断言(49 断言全绿),独立复跑。
- **#3 指标聚合体系完成**(3a 管道 + 3b 读视图)。GroupSummary/GroupInfo 归 #8。
- 计划:`docs/superpowers/plans/2026-07-09-3b-metrics-read-views.md`。

### 2026-07-09 · #3/3a 指标聚合管道 — PR #11
- 两日聚合表 `device_daily_metrics`(PK date+client+project)/`rpc_daily_metrics`(PK date+project+action+client,client_id='' 表无设备),对齐老系统;drop 没用的 `metrics` 空壳。派生/日志型,无 description/deleted_at。
- 完成累加 `MetricsService.recordCompletion`(per-completion upsert,ok/timeout/failed 归类 + total_latency 累加 + max GREATEST),挂 `RequestLogProcessor`;**去重**:`writeSpine` 返回首插标志,仅首见 requestId 才累加(BullMQ 重试不重复计)。
- 重启对账 `rebuildRecent(rawRetentionDays)`:worker 启动删+从 request_logs 重灌(UTC 分桶,同增量口径),修漂移。
- 按天清理:maintenance `metrics-cleanup`(5min)删 stat_date 超 `aggregateRetentionDays`(默认 30)。
- 验证:build/lint/format 绿;内部检查现为 `test:integration:metrics`(累加+对账 11 断言);公开读面由黑盒 E2E 验证。
- **读视图=3b**;GroupSummary/GroupInfo=#8。
- 计划:`docs/superpowers/plans/2026-07-09-3a-metrics-aggregation-pipeline.md`。

### 2026-07-09 · #2/2d 设备持久态 — PR #10(device-model epic 收尾)
- `devices` 落齐 `status`(online/offline/stale)/`platform`/`last_ip`/`extra`;上线从连接捕获 platform(`?platform`)/ip(socket x-forwarded-for→remoteAddress)/extra(`?extra`)+ 置 status=online;优雅下线置 offline。
- stale 扫描 worker(maintenance 队列 `mark-devices-stale`,60s):PG online=true 但 Redis `presence:{clientId}` 已过期 → 置 offline/stale(`DevicesService.markStaleOffline`,注入 Redis,无 WsModule 环)。
- 设备列表/详情 API `/devices`(`read/device`,→14 条),mirror monitor read 模式。
- 验证:build/lint/format 绿;e2e smoke 加 5 条设备态断言全绿;stale 算法内部检查现为 `test:integration:device-stale`。
- **device-model epic(#2)至此完成**:2a rename → 2b device token → 2c 自注册删 client-login → 2d 持久态。
- 计划:`docs/superpowers/plans/2026-07-09-2d-device-persistent-state.md`。

### 2026-07-09 · #2/2c 设备自注册 + 删 client-login — PR #9
- WS 网关改 device-token 鉴权(`?token=<dk_>&clientId=<自生成>`)→ cache-aside 校验(fail-open,mirror AccessTokenGuard)→ 取 token 的 projectIds → `DevicesService.registerOnline`(upsert client_id/device_token_id/online/last_seen)+ presence.online + welcome;下线 presence.offline + `markOffline`。
- 删旧 client-login:`clients`/`client_groups` 两表(破坏式 drop 迁移 `0002`)+ `src/application/client/`(7 文件)+ `POST/GET /clients`/`POST /api/client/login` + ClientService/Controller/Module + `ProjectsService.projectsOfClient` + seed 的 demo 设备 + `read/create client` 权限(→13 条)。
- 补 2b 延后项:device-token WS 校验缓存 + revoke/delete 缓存失效 + `devices.device_token_id` 索引。
- WsModule 去 JwtModule、import DeviceTokenModule+DevicesModule(无环)。
- 验证:build/lint/format 绿;full e2e smoke 重写为 device-token 自注册闭环,**38 断言全绿**(welcome 带继承 projects、invoke 闭环、注册 token onlineDeviceCount=1)。Sonnet 终审 clean。
- 计划:`docs/superpowers/plans/2026-07-09-2c-device-self-register.md`。

### 2026-07-09 · #2/2b device token — PR #8
- 新表 `device_tokens`(镜像 access_tokens,token 前缀 `dk_`,实体表带 description+软删+partial unique)+ `device_token_projects`(M2M)。
- CRUD `/device-tokens`(建返明文/列表含 project 名+在线设备数/撤销/软删),权限 `manage/device-token`(admin isRoot 直通),1:1 镜像 access-token 模块。
- `devices` 加可空 `device_token_id` FK(2c 自注册写值,现在线数恒 0)。
- 增量迁移 `0001_simple_klaw`;seed 加 `manage/device-token`(权限 15 条)。
- 2b 无 Redis(无消费方);revoke/delete 留 `ponytail:` 注释,2c 加 WS 校验缓存时补缓存失效。
- 验证:build/lint/format 绿;full e2e `smoke` 39 断言(含 device-token CRUD 6 条)全绿。
- 计划:`docs/superpowers/plans/2026-07-09-2b-device-token.md`。用 subagent-driven-development 执行(Sonnet 实现 subagent × 3 task + Opus review)。

### 2026-07-09 · #2/2a rename groups→projects — PR #7
- 库+码彻底改名:表 `groups→projects`、`access_token_groups→access_token_projects`(列 `group_id→project_id`)、`request_logs.group_name→project_name`、`metrics.group_name→project_name`;Redis 键 `group:clients→project:clients`;invoke 路由 `/:group→/:project`;权限 subject `group→project`(元组 3 条);WS 协议 job/welcome 字段;Manticore DDL/doc `group_name→project_name`;rpc 内部状态 `no_group→no_project`。
- Nest 模块 `groups/→projects/`(`Projects{Service,Controller,Module}`、`CreateProjectDto`)。
- `client_groups` 表 + `group_id` 列保留(2c 随 client-login 删),仅 FK 指向改名后的 `projects`;删死脚本 `migrate-groups.ts` + `migrate:groups` npm script。
- 迁移 squash 破坏式重建为单个 `0000_cold_zaladane.sql`;dev 库 drop+重建+seed;`db:generate` 无残差。
- 验证:`pnpm build/lint/format` 绿;内部保留检查现为 `test:integration:retention`;full e2e 当时 35 断言全绿。
- 计划:`docs/superpowers/plans/2026-07-09-2a-rename-groups-to-projects.md`。

### 2026-07-08 · #1 日志保留/裁剪 — PR #5
- `RequestLogsService.cleanupOldRequests(days)` 按天硬删 + `trimScopes(keep)` 窗口函数按 scope 裁剪(每 `(group,action,client)` 留最新 100,`client_id` NULL 归同一 scope)。
- 复用 5min 维护 worker:`WorkerBootstrap` 加 `retention-sweep`,`MaintenanceProcessor` 按 `job.name` 分派。
- zod 配置 `retention.rawRetentionDays`(3)/ `keepLatestPerScope`(100),fail-fast。
- 内部集成检查 `src/scripts/retention.integration.ts`(retention 无公开控制面，明确非 E2E)。
- 计划:`docs/superpowers/plans/2026-07-08-request-log-retention.md`。
