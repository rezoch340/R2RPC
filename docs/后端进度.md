# RER0RPC 后端进度台账

> **唯一进度真源。** 对齐老 Go 系统 `RER0RPC-核心功能统计.md` 的完整语义,记录后端还差哪些能力、做到哪了。
>
> **更新规则(进度永不丢):** 每个功能在自己的分支上做,**完成一项就在本文件把它勾成 ✅ 并补「完成记录」**,和代码一起进同一个 PR 合入 main。进度跟着 git 走,不依赖会话记忆。
>
> 详细语义参考:`docs/RER0RPC-核心功能统计.md`;分布式/冷热路径约定:`docs/design-conventions.md`。范围(不做 SDK/monorepo,frontend 独立):`docs/RER0RPC-新版开工提示词.md`。

**图例:** ✅ 已完成 · 🚧 进行中 · ⬜ 待办

---

## 总览

| # | 事项 | 优先级 | 状态 | 依赖 |
|---|------|--------|------|------|
| — | MVP 调用闭环 + 三套授权域(设备组/RBAC/access token)+ 全局软删除 | — | ✅ | — |
| 1 | 日志保留/裁剪 | 高 | ✅ | — |
| 2 | 设备持久态 | 高 | ⬜ | — |
| 3 | 指标聚合体系 | 高 | ⬜ | #2 |
| 4 | maxInFlight 在途并发限流 | 高 | ⬜ | — |
| 5 | WS 服务端 ping + 读超时判离线 | 中 | ⬜ | — |
| 6 | WS 单帧上限 + 拒分片 + deadline 过期丢弃 | 中 | ⬜ | — |
| 7 | 用户 enabled + last_login_at | 中 | ⬜ | — |
| 8 | 分组 enabled + GroupInfo 派生统计 | 中 | ⬜ | #2,#3 |
| 9 | 监控筛选下拉选项 | 低 | ⬜ | — |
| 10 | rpc 状态细分 `rejected` | 低 | ⬜(其余已完成) | #4 |
| 11 | client login `platform`/`extra` 落库 | 低 | ⬜ | #2 |

---

## 大块(核心能力)

### ✅ #1 日志保留/裁剪
`request_logs` 是日志表(硬删)。按天清理(默认 3 天)+ 每 `(group,action,client)` scope 只留最新 100 条(窗口函数),挂现成 5min 维护 worker(`retention-sweep`)。阈值走 zod 配置。
> **聚合表按天清理**(`AGGREGATE_RETENTION_DAYS`,§2.8 item 3)暂缓 → 并入 #3(表还没建)。

### ⬜ #2 设备持久态
**现状(已核实):** `devices.service.ts`/`devices.controller.ts` 各 4 行、0 方法,纯空壳。在线态只活在 Redis presence(TTL),未落库,无 stale 扫描,无设备列表/详情 API。
**要做:** `devices` 表落 user_id 归属 / platform / status(online·offline)/ last_seen_at / last_ip / extra_json;`MarkStaleDevicesOffline` 扫描(`last_seen_at < now-grace` 置离线);设备列表/详情 API。**PG `devices.status` 是权威在线态,Redis 只是 TTL 旁路镜像。**
**解锁:** #3(device 日聚合)、#11(extra_json)。

### ⬜ #3 指标聚合体系
**现状(已核实):** `metrics.service` 43 行,全是查询时现扫 `request_logs`;`device_daily`/`rpc_daily` 聚合表未建,`metrics` 表空壳。
**要做:** 两张日聚合表 + worker 累加写入(complete/rpc_metric/device_metric)+ 重启对账(从 request_logs 重灌最近 N 天)+ 周/趋势/GroupSummary 派生 + 聚合表按天清理(留 30 天,承接 #1 暂缓项)。
**依赖:** #2(设备聚合口径)。

### ⬜ #4 maxInFlight 在途并发限流
**现状:** 未做。设备在线数不限,但单设备同时在途任务数无上限,高并发下会压垮单机(轮询摊得均也没用)。
**要做:** client login 收 `maxInFlight`(夹取 [256,1024])+ WS 单设备在途计数上限,满则 `rejected`/429。
**解锁:** #10 的 `rejected` 状态。

## WS 协议健壮性

### ⬜ #5 服务端主动 ping + 读超时
现在纯靠客户端心跳 + Redis presence TTL。补:服务端 ping(5s)+ 读超时(20s)判离线。

### ⬜ #6 帧限制 + deadline
单帧 4 MiB 上限、收到分片帧(FIN=0)直接断、`job.deadlineAt` 过期任务丢弃。均未做。

## 账户 / 分组管理

### ⬜ #7 用户 enabled + last_login_at
用户 `enabled`(禁用登录)+ `last_login_at`(登录更新)。(`can_rpc` 已被 RBAC 取代,算演进。)

### ⬜ #8 分组 enabled + GroupInfo
分组 `enabled`(启停,登录/派发校验)+ GroupInfo 派生统计(设备数/在线数/近7天请求/成功率/运行态 disabled·no_device·online·stale·offline)。
**依赖:** #2、#3。

## 小项

### ⬜ #9 监控筛选下拉选项
从日志去重取 group/action/client 供 UI 下拉,且要求实体仍存在。

### ⬜ #10 rpc 状态细分 `rejected`
**现状(已核实):** 已有 `ok/error/timeout/no_group/no_device/offline/unavailable`。只差 `rejected`(429 队列满/组饱和),**依赖 #4**。

### ⬜ #11 client login platform/extra 落库
client login 的 `platform`/`extra` 透传落 `devices.extra_json`。**依赖 #2。**

---

## 已知小遗留(非阻塞,代码内留注释)
- `clientQueue` 的 `?clientId` 跨组在线态探测(仅泄露一个布尔)。
- 每请求权限/isRoot 查询可加短 TTL cache-aside(`jwt.strategy.ts` 有注释)。

---

## 完成记录(倒序)

### 2026-07-08 · #1 日志保留/裁剪 — PR #5
- `RequestLogsService.cleanupOldRequests(days)` 按天硬删 + `trimScopes(keep)` 窗口函数按 scope 裁剪(每 `(group,action,client)` 留最新 100,`client_id` NULL 归同一 scope)。
- 复用 5min 维护 worker:`WorkerBootstrap` 加 `retention-sweep`,`MaintenanceProcessor` 按 `job.name` 分派。
- zod 配置 `retention.rawRetentionDays`(3)/ `keepLatestPerScope`(100),fail-fast。
- 独立冒烟 `src/scripts/retention-smoke.ts`(retention 无 API 面 → 直连 PG)。
- 计划:`docs/superpowers/plans/2026-07-08-request-log-retention.md`。
