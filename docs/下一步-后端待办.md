# R2RPC 后端下一步

> 更新日期：2026-07-25。后端功能 backlog #1–#15、管理前端 #16、手动 RPC 调试 #17、
> 后台授权缓存 #18、统一配置与完整 Compose #19、容器性能验收 #20、Android 与
> JavaScript SDK #21 已收官；本文只记录发布
> 就绪和非阻塞硬化工作。

## 当前基线

- API/Worker 双进程已完成。
- 设备模型、三套鉴权、两类令牌作用域二次编辑、19 条带说明内置权限、权限组、系统操作审计、
  管理员账号隔离与改密、公开/手动 RPC、AppAudit 日志 Step、指标、限流和 WS 健壮性已完成。
- OpenAPI 已导出，共 39 个 HTTP 路径模板、51 个操作；全部具有详细说明、成功响应 schema、
  标准错误响应，并区分后台 JWT 与调用方 Access Token。
- 安全策略、贡献指南、行为准则、许可与上游归属、Issue/PR 模板和发布流程已经补齐；公共
  SDK 发布仍受 `UNLICENSED` 权利边界约束。
- 黑盒完整性冒烟为 172 项，全部通过 HTTP/WebSocket，不直连数据库、Redis、Manticore 或应用内部服务。
- 后台授权使用公共 Redis cache-aside，默认 TTL 60 秒；Redis fallback/回写与数据库写后失效均已完成。
- 活跃源码与测试已完成变量命名/复杂度审计；`pnpm lint:check` 强制完整变量名、复杂度 ≤ 10、嵌套 ≤ 3、单函数语句 ≤ 40。
- 管理前端已覆盖全部后台公开面，具有独立命名门禁、生产构建、容器和 12 项纯接口
  Playwright 冒烟；手动 RPC、筛选分页、令牌作用域编辑、复制回退和日志详情交互均已覆盖。
- API、Worker、迁移、种子和前端已统一使用 `config.yaml` schema；完整 Compose、生产镜像、
  一次性迁移/种子任务与启动依赖已完成。
- 容器内固定速率性能测试已完成，默认挂 4 台 WS 设备并混合 10 个公开接口场景；隔离实测
  1600 请求、0 失败、80.03 req/s、P95 7.50 ms，全部设备均收到 Hello。Compose 服务资源
  上限合计 4 核、3840 MiB。
- Android/Kotlin 与 JavaScript/TypeScript SDK 已完成设备端、调用方与 AppAudit V1
  Recorder；JavaScript 10 项、Android 8 项 SDK 测试通过。

## P0：发布就绪

1. **CI**
   - build
   - `pnpm lint:check`（命名 + ESLint）
   - Prettier check
   - Jest
   - 隔离基础设施上的 `pnpm smoke`
2. **健康检查**
   - API liveness
   - PostgreSQL/Redis/Manticore readiness
   - Worker 存活和队列消费状态
3. **Kubernetes / 外部编排**
   - 从现有 Compose 契约派生 Deployment、Job、ConfigMap/Secret 示例
   - API 与 Worker 独立扩缩容
4. **生产密钥**
   - 生产 secret 注入
   - TLS/WSS 和反向代理说明

## P1：边界硬化

1. 限制 token 列表回显明文的后台权限和审计策略。
2. 为 WS 内部 `receiver.getInfo` 兼容性增加升级回归测试。

## P2：可观测性与运维

1. Prometheus 指标。
2. 结构化日志与 requestId。
3. BullMQ backlog、failed/dead-letter 告警。
4. PostgreSQL/Manticore 保留任务运行结果审计。
5. 多 API 实例跨节点黑盒测试。
6. 前端错误监控、Web Vitals 和可访问性自动检查。

## 测试规则

- `pnpm smoke` / `pnpm test:e2e` 只能访问 HTTP/WS。
- E2E 与性能执行器禁止导入数据库、Redis、Drizzle 或 Nest 应用内部 service。
- Worker 结果通过 `/monitor/*`、`/metrics/*` 等公开读接口观察。
- 性能测试同样只能访问公开 HTTP/WebSocket，业务响应、设备覆盖或质量阈值失败必须非零退出。
- 无公开控制面的底层算法检查放在 `pnpm test:integration:*`，不得称为 E2E。

## 下一项建议

优先实现 **P0 CI + 专用健康检查**。完整容器与 Compose 已落地，下一步把现有发布路径纳入
自动门禁并补 `/health`、`/ready` 与 Worker readiness。
